SELECT a2, b2 FROM t2
UNION ALL
SELECT a2, c2 FROM t2
UNION ALL
(
   SELECT b1, c1 FROM t1
   WHERE a1 < 0
   INTERSECT ALL
   SELECT b1, c1 FROM t1
   WHERE a1 > 0
   EXCEPT ALL
   SELECT b1, c1 FROM t1
   WHERE a1 > 0
)
